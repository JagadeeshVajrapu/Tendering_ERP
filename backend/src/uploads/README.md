# Runtime uploads directory

Actual uploaded files are stored at `backend/uploads/` (project root, sibling to `src/`).
Configure via `UPLOADS_DIR` in `.env`. Subdirectories: `tenders/`, `reports/`, `temp/`.
